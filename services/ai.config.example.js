// Copy this file to ai.config.js (same folder) and fill in your own key.
// ai.config.js is gitignored — never commit a real key.
//
// Get an API Key from the 百度千帆 console: https://console.bce.baidu.com/qianfan/
// (v2 API uses a single Bearer API Key, no separate secret/token exchange.)

export const AI_CONFIG = {
  apiKey: '',
  // Pick a model your account actually has access to in the Qianfan console.
  model: 'ernie-4.5-turbo-128k'
}
